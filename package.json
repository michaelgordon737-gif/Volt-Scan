{
  "name": "voltscan",
  "version": "0.2.0",
  "private": true,
  "description": "Phone-friendly stock volatility scanner with watchlists, top gainers, alerts and PWA support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
